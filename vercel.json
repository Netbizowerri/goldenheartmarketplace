{
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node",
      "config": {
        "command": "npm run build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.ts"
    }
  ]
}
